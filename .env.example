# Server Configuration
PORT=3000
NODE_ENV=development

# Optional: Set base URL for QR code generation (defaults to http://localhost:3000)
# BASE_URL=https://your-domain.com

# Database (SQLite is local, no configuration needed)
# Database file will be created at server/momentum.db
